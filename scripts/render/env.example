# Copy this to scripts/render/.env (or export vars in your shell)
# Do NOT commit real tokens.

# Render API token (keep secret)
RENDER_API_TOKEN=

# Workspace (owner) ID
OWNER_ID=

# Service ID of your API service
SERVICE_ID=

# Optional defaults
# Minutes of history to pull for logs
START_MINUTES=60
# Path to filter 5xx request logs (URL path only)
PATH_FILTER=/v2/api/generate-world
