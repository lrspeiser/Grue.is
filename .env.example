# Environment variables for Render deployment

# OpenAI API Key (required)
OPENAI_API_KEY=your_openai_api_key_here

# Database URL (set by Render for internal DB; for local dev, use External Database URL)
# Example (replace with your own credentials):
# DATABASE_URL=postgresql://grue_user:password@dpg-xxxxx.oregon-postgres.render.com/grue
DATABASE_URL=

# Force SSL for local connections to hosted providers (Render external URLs require SSL)
# Set to true when using a hosted Postgres from your machine
DB_SSL=true

# Set to true to disable Postgres and use an in-memory dev DB (no persistence)
DISABLE_DB=false

# Node environment
NODE_ENV=production

# Server port (automatically set by Render)
PORT=3000
